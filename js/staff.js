var myObject = { name : "Martin", email: "myemail@company.com"};
console.dir(myObject);

fetch(url)
  .then(response => {
    // Handle the response here
  })
  .catch(error => {
    // Handle errors here
  });